┌─ 前端 ─────────────────────────────
React 19 + TypeScript
Vite
Tailwind CSS + shadcn/ui

状态与请求
├─ Zustand（全局状态：主题/当前会话/窗口状态）
└─ TanStack Query（请求缓存、loading、重试）

Markdown 渲染
├─ react-markdown
├─ remark-gfm（表格）
├─ rehype-highlight（代码高亮）
└─ remark-math + rehype-katex（如需公式，按需加）

路由
└─ React Router

┌─ 后端 ─────────────────────────────
Python 3.12 + FastAPI + Uvicorn
Pydantic + pydantic-settings（数据校验/配置管理）
SQLModel + SQLite（本地数据库）
httpx（HTTP 客户端）
loguru（日志）

┌─ 桌面外壳 ──────────────────────────
Tauri 2.0

开发阶段：uv 管理 Python 环境，sidecar 直接跑源码
发布阶段：PyInstaller 打包 → Tauri Bundler 缝合成 .exe/.dmg